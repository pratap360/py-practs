# how to import and works ?
# never keep the file name as modules names
# because it find the modules in local directores
# eg of code
#sklearn is a modules
from sklearn.ensemle import random forest classifier
print (randomforestclassifier())

import file2
print(file2.a)
file2.printjoke("this is me")
